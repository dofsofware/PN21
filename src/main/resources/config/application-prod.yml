logging:
  level:
    ROOT: INFO
    io.github.jhipster: INFO
    com.secusociale.portail: INFO

management:
  metrics:
    export:
      prometheus:
        enabled: false

spring:
  devtools:
    restart:
      enabled: false
    livereload:
      enabled: false
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    url: jdbc:mysql://localhost:3306/cssipres?useUnicode=true&characterEncoding=utf8&useSSL=false&useLegacyDatetimeCode=false&serverTimezone=UTC&createDatabaseIfNotExist=true #prod
    #url: jdbc:mysql://localhost:3306/newcssipres?useUnicode=true&characterEncoding=utf8&useSSL=false&useLegacyDatetimeCode=false&serverTimezone=UTC&createDatabaseIfNotExist=true #preprod
    username: root
    #password: mysql@SUN2020
    #password: Oracle123$
    password: Admin2468nko
    #jndi-name: 'java:/cssipres'
    hikari:
      poolName: Hikari
      auto-commit: false
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate.id.new_generator_mappings: true
      hibernate.connection.provider_disables_autocommit: true
      hibernate.cache.use_second_level_cache: true
      hibernate.cache.use_query_cache: false
      hibernate.generate_statistics: false
      # modify batch size as necessary
      hibernate.jdbc.batch_size: 25
      hibernate.order_inserts: true
      hibernate.order_updates: true
      hibernate.query.fail_on_pagination_over_collection_fetch: true
      hibernate.query.in_clause_parameter_padding: true
      hibernate.javax.cache.missing_cache_strategy: create
  # Replace by 'prod, faker' to add the faker context and have sample data loaded in production
  liquibase:
    contexts: prod
  mail:
    host: smtp.gmail.com
    #    host: smtp.office365.com
    port: 587
    #port: 465
    username: sender.suntelecoms@gmail.com
    #username: cssipresportail@secusociale.sn
    #password: ekozbcmeulfvdmzv
    password: npscmnrysimbpubc
    #password: RVpho023
    protocol: smtp
    tls: true
    auth: true
    from: sender.suntelecoms@gmail.com
    properties.mail.smtp:
      auth: true
      starttls.enable: true
      ssl.trust: smtp.gmail.com
  thymeleaf:
    cache: true
  sleuth:
    sampler:
      probability: 1 # report 100% of traces
  zipkin: # Use the "zipkin" Maven profile to have the Spring Cloud Zipkin dependencies
    base-url: http://localhost:9411
    enabled: false
    locator:
      discovery:
        enabled: true
  servlet:
    multipart:
      max-file-size: 400MB
      max-request-size: 400MB
# ===================================================================
# To enable TLS in production, generate a certificate using:
# keytool -genkey -alias portailcssipresv2 -storetype PKCS12 -keyalg RSA -keysize 2048 -keystore keystore.p12 -validity 3650
#
# You can also use Let's Encrypt:
# https://maximilian-boehm.com/hp2121/Create-a-Java-Keystore-JKS-from-Let-s-Encrypt-Certificates.htm
#
# Then, modify the server.ssl properties so your "server" configuration looks like:
#
# server:
#    port: 443
#    ssl:
#        key-store: classpath:config/tls/keystore.p12
#        key-store-password: password
#        key-store-type: PKCS12
#        key-alias: portailcssipresv2
#        # The ciphers suite enforce the security by deactivating some old and deprecated SSL cipher, this list was tested against SSL Labs (https://www.ssllabs.com/ssltest/)
#        ciphers: TLS_ECDHE_RSA_WITH_AES_128_CBC_SHA,TLS_ECDHE_RSA_WITH_AES_256_CBC_SHA,TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256,TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384 ,TLS_DHE_RSA_WITH_AES_128_GCM_SHA256 ,TLS_DHE_RSA_WITH_AES_256_GCM_SHA384 ,TLS_ECDHE_RSA_WITH_AES_128_CBC_SHA256,TLS_ECDHE_RSA_WITH_AES_256_CBC_SHA384,TLS_DHE_RSA_WITH_AES_128_CBC_SHA256,TLS_DHE_RSA_WITH_AES_128_CBC_SHA,TLS_DHE_RSA_WITH_AES_256_CBC_SHA256,TLS_DHE_RSA_WITH_AES_256_CBC_SHA,TLS_RSA_WITH_AES_128_GCM_SHA256,TLS_RSA_WITH_AES_256_GCM_SHA384,TLS_RSA_WITH_AES_128_CBC_SHA256,TLS_RSA_WITH_AES_256_CBC_SHA256,TLS_RSA_WITH_AES_128_CBC_SHA,TLS_RSA_WITH_AES_256_CBC_SHA,TLS_DHE_RSA_WITH_CAMELLIA_256_CBC_SHA,TLS_RSA_WITH_CAMELLIA_256_CBC_SHA,TLS_DHE_RSA_WITH_CAMELLIA_128_CBC_SHA,TLS_RSA_WITH_CAMELLIA_128_CBC_SHA
# ===================================================================
server:
  port: 8080
  compression:
    enabled: true
    mime-types: text/html,text/xml,text/plain,text/css, application/javascript, application/json
    min-response-size: 1024

portail:
#  url: https://bo.secusociale.suntelecoms.com/factures-impayees #sunpreprod
  url: https://online.secusociale.sn/cssipres/#/factures-impayees #preprod
  #url: https://online.secusociale.sn/cssipres/#/factures-impayees #prod
# ===================================================================
# JHipster specific properties
#
# Full reference is available at: https://www.jhipster.tech/common-application-properties/
# ===================================================================

jhipster:
  cors:
    allowed-origins: "*"
    allowed-methods: "*"
    allowed-headers: "*"
    exposed-headers: "Authorization,Link,X-Total-Count"
    allow-credentials: true
    max-age: 1800
  http:
    cache: # Used by the CachingHttpHeadersFilter
      timeToLiveInDays: 1461
  cache: # Cache configuration
    ehcache: # Ehcache configuration
      time-to-live-seconds: 3600 # By default, objects stay 1 hour in the cache
      max-entries: 1000 # Number of objects in each cache entry
  security:
    authentication:
      jwt:
        # This token must be encoded using Base64 and be at least 256 bits long (you can type `openssl rand -base64 64` on your command line to generate a 512 bits one)
        # As this is the PRODUCTION configuration, you MUST change the default key, and store it securely:
        # - In the JHipster Registry (which includes a Spring Cloud Config server)
        # - In a separate `application-prod.yml` file, in the same folder as your executable JAR file
        # - In the `JHIPSTER_SECURITY_AUTHENTICATION_JWT_BASE64_SECRET` environment variable
        base64-secret: Y2Y4NjNhODE3YjU1M2FhMzQwYTA0ZTQwZjFiMWNmMDEwZjBmNTUzYWY1ZjJmZGJiYzQzM2I0MmZkZjZjNTBjYmY3YzRmZTFmNmE4YjVlYzU1NDRkOGVjODE1MDI3NWQ2OWUxZTdhN2Y1YjU2NTE3MGJiMjk2NDM1NjUxMjAwYjU=
        # Token is valid 24 hours
        token-validity-in-seconds: 86400
        token-validity-in-seconds-for-remember-me: 86400
  mail: # specific JHipster mail property, for standard properties see MailProperties
    #base-url: https://api.secusociale.suntelecoms.com # Modify according to your server's URL #preprod
    base-url: https://online.secusociale.sn # Modify according to your server's URL #prod
  metrics:
    logs: # Reports metrics in the logs
      enabled: false
      report-frequency: 60 # in seconds
  logging:
    use-json-format: false # By default, logs are not in Json format
    logstash: # Forward logs to logstash over a socket, used by LoggingConfiguration
      enabled: false
      host: localhost
      port: 5000
      queue-size: 512
  audit-events:
    retention-period: 30 # Number of days before audit events are deleted.

  # ===================================================================
  # Application specific properties
  # Add your own application properties here, see the ApplicationProperties class
  # to have type-safe configuration, like in the JHipsterProperties above
  #
  # More documentation is available at:xit

  # https://www.jhipster.tech/common-application-properties/
  # ===================================================================
  # ====================     ~10.90.100.23:7500~          ===============================================

application:
  docdir: /opt/tomcat/webapps/documents #preprod
  docuri: https://online.secusociale.sn/documents #preprod
  #docdir: /opt/tomcat/webapps/documents #prod
  #docuri: https://online.secusociale.sn/documents #prod
  #docdir: /var/www/html/cssipres #prodsun
  #docuri: http://74.208.120.109:8077/cssipres/documents #prodsun
  #ftpaddress: 10.90.100.23 #prod
  ftpaddress: 192.168.125.23 #preprod & #prod
  ftpport: 22
  ftplogin: wango
  ftppass: wango
  ftppath: /app10/Oracle/Applications/declaration/DNSExcel
---
cssipres:
  #docdir: /opt/tomcat/webapps/documents #prod
  #dochost: https://online.secusociale.sn/documents #prod
  docdir: /opt/tomcat/webapps/documents #preprod
  dochost: https://online.secusociale.sn/documents #preprod
  #docdir: /var/www/html/cssipres #prodsun
  #docuri: http://74.208.120.109:8077/cssipres/documents #prodsun
---
constantes:
  nbPoints: 10000
